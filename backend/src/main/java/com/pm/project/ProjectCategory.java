package com.pm.project;

/** Top-level grouping shown in the sidebar. New categories can be added without code changes elsewhere. */
public enum ProjectCategory {
    APPLICATION,
    DATABASE,
    SCRIPT,
    OTHER
}
